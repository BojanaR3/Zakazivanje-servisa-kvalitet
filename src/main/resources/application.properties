# ======================
#  DATA SOURCE (MySQL)
# ======================
spring.datasource.url=jdbc:mysql://localhost:3306/zakazivanje_servisa_njt?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:root}
# ======================
#  JPA / HIBERNATE
# ======================
spring.jpa.hibernate.ddl-auto=update
#spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
# ======================
#  SERVER / DEV
# ======================
server.error.include-message=always
# server.port=8080
# ======================
#  CORS (ako \u017eeli\u0161 i kroz properties)
#  (ostaje i Java CORS u SecurityConfig)
# ======================
# spring.web.cors.allowed-origins=http://localhost:3000
# spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
# spring.web.cors.allowed-headers=*
# ======================
#  JWT
#  - obavezno >32 char za HS256 klju\u010d
# ======================
app.jwt.secret=${JWT_SECRET:ChangeThisSuperSecretKey_AtLeast32Chars_Long___123}
app.jwt.expiration-ms=86400000
spring.mail.host=sandbox.smtp.mailtrap.io
spring.mail.port=2525
spring.mail.username=${MAIL_USERNAME:f575740ceb3248}
spring.mail.password=${MAIL_PASSWORD:b62422536912ce}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
# ko \u0161alje mejl (from polje)
app.mail.from=ServisApp <noreply@servisapp.local>
app.frontend.url=http://localhost:3000
logging.level.org.springframework.security=DEBUG